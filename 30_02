#include <stdio.h>
#include<stdlib.h>

int main()
{ 
	float height, sex, weight;
	
	scanf("%f %f", &height, &sex);

	if(sex==1) weight = (height-80)*0.7;
	if(sex==2) weight = (height-70)*0.6;
 	
 	printf("%.1f", weight);
 	
 return 0;  
}
