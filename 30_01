#include <stdio.h>
#include<stdlib.h>

int main()
{ 
	int i, n, d=0;
	
	scanf("%d", &n);
	
	for (i=1; i<=n; i++){
		if(n%i==0) d=d+1;
	}
	
 	if (d==2) printf("Yes");
 	else printf("No");
 
 return 0;  
} 
