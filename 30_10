#include <stdio.h>
#include<stdlib.h>
#include<math.h>

int main()
{ 
	float x, y;
	double X, Y;
	
	scanf("%f %f", &x, &y);
	X = pow(x, 2);
	Y = pow(y, 2);
	
	if((X+Y)>10000) printf("Outside");
	else printf("Inside");
 
 return 0;  
}
